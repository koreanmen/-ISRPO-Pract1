<Window x:Class="_ISRPO_Pract1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_ISRPO_Pract1"
        mc:Ignorable="d"
        Title="Сумма столбцов с нулями" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="n (строк): " VerticalAlignment="Center"/>
            <TextBox x:Name="TbRows" Width="50" Margin="5"/>
            <TextBlock Text="m (столбцов): " VerticalAlignment="Center"/>
            <TextBox x:Name="TbCols" Width="50" Margin="5"/>
            <Button Content="Создать матрицу" Click="BtnCreateMatrix_Click" Margin="10,0" Padding="10,5"/>
        </StackPanel>

        <DataGrid x:Name="DgMatrix" Grid.Row="1" AutoGenerateColumns="True" 
                  CanUserAddRows="False" Margin="0,5" Height="200"/>

        <Button Grid.Row="2" Content="Вычислить сумму столбцов с нулями" Height="50"
                Click="BtnCalculate_Click" Margin="0,10" Padding="10,5" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="3" x:Name="TbResult" Margin="0,10" FontSize="16" FontWeight="Bold"/>
    </Grid>
</Window>
